# Telegram Stars Bot (AIogram 3 + FastAPI)

Бот работает как внутренний кошелёк Stars: покупка пакетов, подарки другим пользователям, просмотр баланса и истории. Внутренний API на FastAPI позволяет проверять балансы и историю через Bearer‑токен.

## Стек
- Python 3.11+, aiogram 3.x (polling)
- FastAPI + Uvicorn
- SQLite + aiosqlite, таблицы создаются при старте
- Структура: `src/bot`, `src/api`, `src/db`, `src/config`

База данных:
- `users` — user_id, username, balance
- `payments` — входящие платежи Stars, `charge_id` уникален, `refunded`
- `transfers` — подарки между пользователями
- `transactions` — история для пользователя (purchase/gift_in/gift_out/refund) с `balance_after`

## Настройка окружения
1) Скопируйте `.env.example` в `.env` и заполните:
```
BOT_TOKEN=xxx           # токен бота от BotFather
API_TOKEN=xxx           # токен для защиты FastAPI
HOST=0.0.0.0
PORT=8000
DB_PATH=stars.db
TEST_MODE=0             # оставить 0, логика тестового режима не нужна
LOG_LEVEL=INFO
```

2) Установите зависимости и запустите:
```bash
pip install -r requirements.txt
python -m src.main
```

### Запуск в Docker
```bash
docker build -t stars-bot .
docker run --env-file .env -p 8000:8000 stars-bot
```
База по умолчанию лежит в контейнере (`/app/stars.db`). При необходимости примонтируйте volume.

## Интерфейс бота
- `/start` — приветствие и меню:
  - ⭐ Купить Stars (пакеты 50/100/250/500; invoice в XTR)
  - 🎁 Подарить Stars (ввод @username/user_id, выбор суммы, списание и зачисление)
  - 💳 Мой баланс (только внутренний баланс бота)
  - 🧾 История (20 последних, пагинация ←/→)
  - ℹ️ Помощь
- Refund: кнопки «↩️ Вернуть N⭐» появляются в истории, если есть подходящий платёж; вызывается `refund_star_payment`, запись помечается refunded в БД.
- Защита от повторной обработки платежа: `charge_id` уникален.

## FastAPI
Все запросы требуют заголовок `Authorization: Bearer <API_TOKEN>`.

- `GET /health`
- `GET /balance/{user_id}`
- `GET /transactions/{user_id}?limit=20&offset=0`
- `GET /top?limit=10`
- `GET /telegram/stars/bot-balance` — прокси к `getMyStarBalance` (если Telegram вернёт ошибку, будет 502)

### Примеры curl
```bash
curl -H "Authorization: Bearer $API_TOKEN" http://localhost:8000/health
curl -H "Authorization: Bearer $API_TOKEN" http://localhost:8000/balance/123456
curl -H "Authorization: Bearer $API_TOKEN" "http://localhost:8000/transactions/123456?limit=20"
curl -H "Authorization: Bearer $API_TOKEN" "http://localhost:8000/top?limit=5"
```

## Сценарий проверки
1) Покупка 100⭐: нажмите «⭐ Купить Stars» → «+100 ⭐», оплатите invoice в Stars. Баланс увеличится на 100.
2) Показать баланс: «💳 Мой баланс» — отобразится внутренний баланс бота.
3) Подарить 50⭐: «🎁 Подарить Stars» → введите `@username` или `user_id` → выберите «Отправить 50 ⭐». Баланс уменьшится на 50, получателю начислится +50.
4) История: «🧾 История» — увидите последние операции с пагинацией и доступными кнопками возврата (если платежи есть).
